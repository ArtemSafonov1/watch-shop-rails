
  <% category.children.each do |sub| %>
    <% cache sub do %>
      <h4>
        <%= link_to sub.title, "/category/#{sub.id}" %>
      </h4>
      <ul>
        <li><p><%= render 'include/menu/menu', category: sub, cached:true %></p> </li>
      </ul>
    <% end %>
  <% end %>